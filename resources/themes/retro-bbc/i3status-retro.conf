# BBC/UNIX Retro-Futuristic i3status Configuration
# Auto-generated by MASH Installer - Retro Theme

general {
    colors = true
    interval = 1
    output_format = "i3bar"
    color_good = "#4E9A06"
    color_degraded = "#C4A000"
    color_bad = "#CC0000"
}

order += "wireless _first_"
order += "ethernet _first_"
order += "battery all"
order += "cpu_usage"
order += "memory"
order += "disk /"
order += "load"
order += "tztime local"
order += "volume master"
order += "media player"

wireless _first_ {
    format_up = "📶 %quality at %essid %ip"
    format_down = "📵 No WiFi"
}

ethernet _first_ {
    format_up = "🌐 %ip (%speed)"
    format_down = "🌐 No Ethernet"
}

battery all {
    format = "🔋 %status %percentage %remaining"
    format_down = "⚡ AC Power"
    last_full_capacity = true
}

cpu_usage {
    format = "CPU: %usage"
    threshold = 80
}

memory {
    format = "RAM: %used/%total"
    threshold = 80
}

disk "/" {
    format = "Disk: %avail"
    threshold = 10
}

load {
    format = "Load: %1min"
}

tztime local {
    format = "🕐 %Y-%m-%d %H:%M:%S"
}

volume master {
    format = "🔊 %volume"
    format_muted = "🔇 Muted"
    device = "default"
    mixer = "PulseAudio"
    mixer_idx = 0
}

media player {
    format = "🎵 %artist - %title (%player)"
    format_down = "🎵 No media"
    player = "spotify,firefox,chrome,chromium,brave,edge,%any"
    status_format = {
        "play" = "▶"
        "pause" = "⏸"
        "stop" = "◼"
    }
}
