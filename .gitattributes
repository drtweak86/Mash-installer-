# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Custom diff driver for Rust to provide better context in diffs
*.rs diff=rust

# Documentation and config
*.md text diff=markdown
LICENSE text
*.toml text
*.json text
*.yml text
*.yaml text

# Shell scripts should always be LF
*.sh text eol=lf

# Binary assets - do not try to text-diff these
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.ttc binary
*.ttf binary
*.woff binary
*.woff2 binary

# Reduce noise in stats for lockfiles
Cargo.lock text -diff
