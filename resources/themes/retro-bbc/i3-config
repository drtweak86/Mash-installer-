# BBC/UNIX Retro-Futuristic i3 Configuration
# Auto-generated by MASH Installer - Retro Theme

# 🎨 Colors (BBC inspired, modernized)
set $bg           #121212  # Almost black
set $fg           #E0E0E0  # Off-white
set $accent       #FF8C00  # Amber accent
set $secondary    #4E9A06  # Retro green
set $alert        #C4A000  # Amber warning
set $error        #CC0000  # Error red
set $info         #3465A4  # Info blue

# 🖥️ Font (Retro but readable)
font pango:Terminus, FontAwesome 10

# ⚡ Performance (Pi 4B optimized)
gaps inner 10
gaps outer 5
smart_gaps on
smart_borders on

# 🎯 Window Behavior (UNIX-style)
focus_follows_mouse yes
mouse_warping output

# 🔄 Layout modes
bindsym $mod+h split h  # Horizontal (like BBC MODE 0)
bindsym $mod+v split v  # Vertical (like BBC MODE 1)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# 🎨 Window Colors (BBC inspired)
client.focused  $accent $bg $fg
client.unfocused $bg $bg $fg
client.urgent   $error $bg $error

# 🚀 Keybindings (UNIX style)
bindsym $mod+Return exec kitty --class retro-term
bindsym $mod+Shift+q kill
bindsym $mod+d exec rofi -show drun -theme retro-bbc

# 🎵 Media Controls (Modern amenity)
bindsym $mod+Shift+m exec playerctl play-pause
bindsym $mod+Shift+, exec playerctl previous
bindsym $mod+Shift+. exec playerctl next

# 🔇 Volume (Modern amenity)
bindsym $mod+F11 exec pactl set-sink-mute @DEFAULT_SINK@ toggle
bindsym $mod+F12 exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym $mod+F10 exec pactl set-sink-volume @DEFAULT_SINK@ -5%

# 💻 Brightness (Pi specific)
bindsym $mod+F5 exec brightnessctl set 5%-
bindsym $mod+F6 exec brightnessctl set 5%+

# 🎯 Workspaces (BBC Mode names)
set $ws1 "1: □ TERM"
set $ws2 "2: □ CODE"
set $ws3 "3: □ WEB"
set $ws4 "4: ▧ MEDIA"
set $ws5 "5: ▨ SYS"
set $ws6 "6: ▤ MISC"
set $ws7 "7: ▥ GAME"
set $ws8 "8: ▦ CHAT"

workspace $ws1 output primary
workspace $ws2 output primary
workspace $ws3 output primary
workspace $ws4 output primary
workspace $ws5 output primary
workspace $ws6 output primary
workspace $ws7 output primary
workspace $ws8 output primary

# 🎨 Window decorations
for_window [class="retro-term"] border pixel 2
for_window [class="Firefox"] border pixel 1
for_window [class="Geany"] border pixel 1

# 🚀 Startup applications
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id xss-lock -- i3lock -c 121212
exec --no-startup-id conky -c ~/.config/conky/retro-bbc.conkyrc
exec --no-startup-id dunst

# 🎨 Bar Configuration (Retro taskbar)
bar {
    position top
    status_command i3status -c ~/.config/i3/i3status-retro.conf
    font pango:Terminus, FontAwesome 9

    colors {
        background $bg
        statusline $fg
        separator $bg
        focused_workspace  $accent $bg $fg
        active_workspace   $secondary $bg $fg
        inactive_workspace $bg $bg $fg
        urgent_workspace   $error $bg $error
    }
}

# 🎨 Retro window decorations
for_window [class="retro-term"] border pixel 2
for_window [class="Firefox"] border pixel 1
for_window [class="Geany"] border pixel 1
