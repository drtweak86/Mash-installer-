[package]
name = "installer-debian"
version = "0.1.0"
edition = "2021"
description = "Debian-specific installer driver"
license = "MIT"

[dependencies]
installer-core = { path = "../installer-core" }
